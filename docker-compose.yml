version: '3.8'

services:
  cineq-dashboard:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        BUILD_CONFIGURATION: production  # Can be changed to staging or development
    container_name: cineq-dashboard
    ports:
      - "80:80"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.cineq-dashboard.rule=Host(`dashboard.cineq.local`)"
      - "traefik.http.services.cineq-dashboard.loadbalancer.server.port=80"
    networks:
      - cineq-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

networks:
  cineq-network:
    driver: bridge
    
volumes:
  cineq-data: